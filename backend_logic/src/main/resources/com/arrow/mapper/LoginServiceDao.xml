<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.arrow.mapper.LoginServiceDao">
    <resultMap type="com.arrow.model.LoginRequestModel" id="loginRequestModel">
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="flagEnabled" column="flag_enabled"/>
        <result property="role" column="role"/>
    </resultMap>

    <select id="checkUserAuthorization" resultType="String">
        SELECT roles.role
        FROM roles
        INNER JOIN enduser
        ON enduser.username = roles.username
        WHERE enduser.username = #{username} and enduser.password = #{password}
    </select>

    <!--    <insert id="checkUserAuthorization">
         &lt;!&ndash;   <selectKey keyProperty="flagEnabled" resultType="Boolean" order="BEFORE">
                SELECT flag_enabled FROM enduser WHERE username = #{username} and password = #{password}
            </selectKey>
            <if test="flagEnabled != null">
                SELECT role FROM roles WHERE username = #{username}
           </if>&ndash;&gt;
            <selectKey keyProperty="role" resultType="int" order="AFTER">
                SELECT role FROM roles WHERE username = #{username}
            </selectKey>
        </insert>-->
</mapper>